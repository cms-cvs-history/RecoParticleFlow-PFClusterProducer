process eflow =  {
	#keep the logging output to a nice level
	include "FWCore/MessageLogger/data/MessageLogger.cfi"

	source = PoolSource
	{
# works with 0_8_0_pre2 !!
           	# untracked vstring fileNames = {'file:/afs/cern.ch/cms/Tutorials/june06/EcalReconstruction/100ele50GeVMixed.root'}
		untracked vstring fileNames = {'file:../../PFProducer/test/singlepi_reco.root'}
		untracked int32 maxEvents = 10
	}
  
	include "RecoEcal/EgammaClusterProducers/data/geometryForClustering.cff"
        include "RecoParticleFlow/PFClusterProducer/data/pFClusterDefaults.cff"


	module o2 = PoolOutputModule
  	{
     		untracked string fileName = "pfclusters.root" 
     		untracked vstring outputCommands = 
     		{
	       		"drop *",
			"keep recoPFRecHits_*_*_*"
     		}
  	}

	module dump = EventContentAnalyzer {}

# 	path p = {dump} 
 	path p = {pfcluster} 
  	endpath outpath = { o2 }
}
